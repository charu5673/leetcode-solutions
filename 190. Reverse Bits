class Solution {
    public int reverseBits(int n) {
        int ans = 0;
        int power = 31;
        while(n > 0) {
            int d = n % 2;
            ans += Math.pow(2, power) * d;
            n /= 2;
            power--;
        }
        return ans;
    }
}
