class Solution {
    public int lengthOfLastWord(String s) {
        int i;
        s = s.trim();
        for(i = s.length() - 1; i >= 0; i--) {
            if(s.charAt(i) == ' ') break;
        }
        return s.length() - i - 1;
    }
}
